node_modules/
dist/
.astro/
.next/
out/
*.tsbuildinfo
.leads-dev.jsonl
.env
.env.*
!.env.example
.DS_Store
.vercel
.env*

# Baseline-pull resume cursor (scripts/baseline-to-db.mjs) — machine-local progress
scripts/.baseline-watermark.local

# Photo-backfill resume cursor (scripts/backfill-photos.mjs) — machine-local progress
scripts/.photo-backfill-watermark.local
scripts/.photo-backfill-watermark.dry.local

# Photo-mirroring verification screenshots — transient, regenerated by scripts/_scratch-shot.mjs
.verify-shots/

server.log

# Loop runtime (control + transcripts) — not committed; JOURNAL.md IS tracked
loop/.lock
loop/STOP
loop/WATCHDOG_STOP
loop/journal/*.log
o.txt
e.txt

# Blog-redesign review screenshots — transient "eyes", regenerated by scripts/probe-*.mjs
docs/blog-redesign/**/*.png
docs/blog-redesign/_*.png

# audit scratch (session artifacts, never committed)
docs/_audit/
scripts/_scratch-*
